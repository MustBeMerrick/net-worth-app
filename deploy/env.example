# Server runtime secrets — copied to ~/apps/net-worth-app/env by `deploy.sh init`.
# Fill in real values on the server; this template holds no secrets.
# DATABASE_URL is intentionally absent: it is pinned in deploy/compose.yml.

# App
APP_URL=http://gmktec.local:3000
SESSION_SECRET=change-me

# Plaid (leave empty until Plaid integration lands)
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
